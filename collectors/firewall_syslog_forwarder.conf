# /etc/rsyslog.d/60-fortinet.conf  (relay: sends CEF to Sentinel AMA)
module(load="imudp") module(load="imtcp")
input(type="imudp" port="514") input(type="imtcp" port="514")
:msg, contains, "FortiGate" action(type="omfwd" target="<AMA_VM_IP>" port="514" protocol="tcp")
& stop
